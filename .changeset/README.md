# Changesets

Add a changeset when a pull request changes either published package. Select
the appropriate semantic version bump and describe the public behavior change.

Package versions are linked, so releasing one package updates both packages.
